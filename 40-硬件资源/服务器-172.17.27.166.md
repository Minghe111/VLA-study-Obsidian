---
title: 服务器-172.17.27.166
tags: [资源, 服务器]
host: 172.17.27.166
ssh_user: liminghe
status: 已通过SSH只读核验
updated: 2026-09-19
---

# 服务器-172.17.27.166

## 连接

```bash
ssh liminghe@172.17.27.166
```

- 来源：用户于 2026-09-19 明确提供。
- 认证方式：密码认证；密码不存入笔记、Git 或长期记忆，登录时交互输入。
- SSH 端口：上方命令使用默认 22，尚未单独确认。
- 网络：私网地址，需要能够访问该网段；VPN、跳板机需求待确认。
- 2026-09-19 10:55–10:58（Asia/Shanghai）通过 SSH 只读核验成功；未部署或修改远端项目。

## 用户资源目录与操作边界

用户于 2026-09-19 明确指定以下范围，现已通过 SSH 确认三个目录均存在、归属 liminghe，realpath 与记录一致：

| 路径 | 用途与限制 |
| --- | --- |
| `/bigdata/liminghe` | 用户工作资源根目录 |
| `/bigdata2/liminghe` | 用户工作资源根目录 |
| `/home/liminghe` | 必要的系统用户文件与用户配置；已确认登录 HOME 为 `/home/liminghe` |

> [!important] 共享服务器操作边界
> 其余资源不属于用户，不得擅自改动。新项目、数据、模型、环境和大文件优先放在上述两个 bigdata 用户目录内。即使有写权限，也不能据此修改其他用户目录、共享软件、系统配置或停止他人进程。确需越界操作时，先说明具体对象与影响并取得明确授权。

每次远端操作先确认主机、当前账户和目标绝对路径；涉及符号链接时核对实际目标。用户目录内也仅修改当前任务所需内容，不将资源归属视为任意删除或覆盖的授权。不得将本地 `/home/admin123` 路径直接套用到本服务器。

## 已核验硬件与系统

核验时间：2026-09-19 10:55–10:58（Asia/Shanghai）。来源：远端 hostname、lscpu、free、nvidia-smi、nvcc、df 和 Python 包元数据。

| 项目 | 实测结果 |
| --- | --- |
| 主机名 / 账户 | `user-4U-GPU-Server` / `liminghe` |
| GPU | 8×NVIDIA GeForce RTX 3090，每卡 24576 MiB（24 GiB） |
| CPU | 2×Intel Xeon Silver 4314 @ 2.40GHz；合计 32 物理核、64 逻辑 CPU |
| 内存 / Swap | 503 GiB / 2 GiB；核验时可用内存约 488 GiB |
| 操作系统 / 内核 | Ubuntu 20.04.6 LTS / 5.15.0-139-generic，x86_64 |
| NVIDIA 驱动 | 535.247.01 |
| nvidia-smi CUDA 字段 | 12.2（驱动能力字段，不是 Toolkit 版本） |
| 默认 shell nvcc | `/usr/bin/nvcc`，10.1.243 |
| 共享 CUDA Toolkit | `/usr/local/cuda-12.8`，12.8.61；`/usr/local/cuda` 与 `/usr/local/cuda-12` 均解析到此处 |
| robotwin 内 nvcc | `/bigdata2/liminghe/envs/robotwin/bin/nvcc`，12.1.105 |

## 存储空间快照

以下为整个挂载分区的空间，**不是用户配额或独占可用容量**；随其他用户使用而变化。

| 用户目录 | 分区 / 文件系统 | 总容量 | 核验时可用 |
| --- | --- | --- | --- |
| `/bigdata/liminghe` | `/dev/sda1`，ext4，挂载 `/bigdata` | 1.8 T | 240 G |
| `/bigdata2/liminghe` | `/dev/sdd1`，ext4，挂载 `/bigdata2` | 3.5 T | 787 G |
| `/home/liminghe` | `/dev/sdc2`，ext4，挂载 `/` | 439 G | 174 G |

## 已发现的软件与工作资源

- Conda 可执行文件：`/bigdata/liminghe/miniconda/bin/conda`，版本 25.9.1；登录 shell 未自动把 conda 加到 PATH。
- `/bigdata/liminghe/miniconda/envs`：fcgym、lerobot、mylerobot、tinyvla、z1_fastsac。
- `/bigdata2/liminghe/miniconda3/envs`：isaaclab；这里只确认环境目录存在，不认定 miniconda3 是完整 Conda 安装。
- `/bigdata2/liminghe/envs/robotwin`：详见 [[服务器172.17.27.166-robotwin]]。
- `/bigdata2/liminghe` 下存在 VLA-benchmark、gsf_vla、dual_rate_smolvla、mica_vla、MagicRobot、libero-para-20260812、isaac_assets。
- `/bigdata/liminghe` 下存在 hf_cache、lerobot4.1、pi0_libero_finetuned、isaacsim、libero_cache_root、heldout_eval 等资源目录。
- 上述目录名仅用于定位；未扫描私人配置内容、数据集内容或他人目录，也未检查所有环境的可运行性。

> [!important] 主机区分
> 本地电脑上的 RoboTwin checkout 和 robotwin 环境不代表本服务器已安装。远端已核验结果见关联环境卡；不得将本地安装状态套用到远端。

关联：[[硬件资源索引]] · [[环境与部署索引]]
