---
title: 服务器172.17.27.166-robotwin
tags: [环境, RoboTwin]
updated: 2026-09-19
status: 版本及CUDA可见性已验证，完整仿真未验证
---

# 服务器172.17.27.166-robotwin

主机：[[服务器-172.17.27.166]]。证据来自 2026-09-19 SSH 只读核验。

## 路径与进入环境

```bash
source /bigdata/liminghe/miniconda/etc/profile.d/conda.sh
conda activate /bigdata2/liminghe/envs/robotwin
cd /bigdata2/liminghe/VLA-benchmark/RoboTwin
```

以上为按已核验路径整理的进入命令；本次检查直接调用环境内 Python，并未测试完整激活流程。

| 项目 | 实测值 |
| --- | --- |
| Python | 3.10.21 |
| torch / torchvision | 2.4.1+cu121 / 0.19.1+cu121 |
| torch.version.cuda | 12.1 |
| torch.cuda.is_available / device_count | True / 8 |
| 环境内 nvcc | 12.1.105 |
| SAPIEN / MPLib | 3.0.0b1 / 0.2.1 |
| PyTorch3D | 0.7.8 |
| CuRobo 发行包 | nvidia-curobo 0.7.8 |
| XPolicyLab | 0.0.1 |
| warp-lang / NumPy / setuptools | 1.12.0 / 1.26.4 / 69.5.1 |

torch 已实际导入并检查 CUDA 可见性；其余包以上述发行包元数据为证据，不等同于扩展加载或仿真测试通过。

## 仓库与资产

- RoboTwin commit：`6dde57155eafa3e4ebf6ad1f93a7cf7d5d41a755`。
- XPolicyLab submodule commit：`fa431ecd893ee706883e64fe5fe1464ec8cd928d`，与父仓库 pin 一致。
- `git status --short` 发现未跟踪文件 `env_cfg/task_config/deployment_smoke.yml`，原样保留，未检查内容或执行。
- `envs/curobo` 存在。
- `assets` 存在，一级目录含 files、background_texture、objects、embodiments、__MACOSX；未验证资产完整性。

## 后续验证边界

编译扩展前检查实际 `which nvcc`、`nvcc --version` 和 `CUDA_HOME`，优先指向本环境的 12.1 工具链。登录 shell 的 `/usr/bin/nvcc` 是 10.1，共享 `/usr/local/cuda` 指向 12.8，不应据路径名推断匹配。

尚未执行：CUDA 算子计算、PyTorch3D/CuRobo 扩展加载、SAPIEN 渲染、完整任务 rollout、多卡训练。未修改共享 CUDA、驱动或任何远端项目文件。
